# homebrew-git
### Wraps `git` brew
Git, for me!

### Aliases
    ct      commit
    rb      interactive rebase
    ps      push
    pl      pull
    co      checkout
    co      checkout
    dfm     diff from master
    last    last change
    some    pull all the fresh stuff down

